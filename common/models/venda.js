'use strict';

module.exports = function(Venda) {
    // var app = require('../../server/server');

    // Venda.teste = function(filter, cb) {
    //     var Venda_recebimento = app.models.Venda_recebimento;
    //     filter.fields = { id: true };
    //     Venda.find(filter, function(err, arr) {
    //         filter = { where: { property: { inq: arr.propToArray('id') } } }
    //         Venda_recebimento.find(filter, function(err, arrRec) {
    //             cb(null, arrRec);
    //         });
    //     });
    // }
  
    // Venda.remoteMethod(
    //     'teste', {
    //       http: { path: '/teste', verb: 'get' },
    //       accepts: { arg: 'filter', type: 'object' },
    //       returns: { type: 'object', root: true }
    //     }
    // );
};